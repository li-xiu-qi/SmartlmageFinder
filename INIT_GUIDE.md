# SmartImageFinder 初始化指南

这个脚本帮助您快速设置 SmartImageFinder 项目。

## 功能

- 读取 `config.example.yaml` 作为配置模板
- 自动下载 JINA CLIP V2 模型到 `models` 目录
- 生成 `config.yaml` 配置文件（模型路径自动更新为绝对路径）
- 可选配置 API 密钥

## 使用方法

```bash
python init_config.py
```

## 工作流程

1. **读取配置模板** - 自动读取 `config.example.yaml` 作为基础配置
2. **选择模型来源** - 支持三种方式获取模型：
   - 使用 `models` 目录下已存在的模型
   - 使用用户指定的本地模型路径（绝对路径）
   - 使用 ModelScope 下载新模型
3. **更新路径** - 自动将模型路径和其他路径转换为绝对路径
4. **生成配置** - 创建 `config.yaml` 文件

## 模型选择

脚本会按以下优先级检查模型：

1. **现有模型检测** - 自动检查 `models` 目录下是否有 `*jina-clip*` 模型
2. **用户选择** - 如果没有现有模型，提供两个选项：
   - 下载模型到本地
   - 使用已有的本地模型路径（需要提供绝对路径）
3. **路径验证** - 如果选择自定义路径，会验证路径是否存在

## 可选配置

脚本会询问是否修改以下配置（可直接回车保持默认值）：

- OpenAI API 密钥
- API Base URL

## 注意事项

- 所有配置基于 `config.example.yaml` 生成，保持一致性
- 首次下载模型需要较长时间，请耐心等待
- 如果已存在模型文件，脚本会询问是否重用
- 生成的配置文件可以后续手动编辑

## 完成后

初始化完成后，您就可以运行项目了：

```bash
python main.py
```
